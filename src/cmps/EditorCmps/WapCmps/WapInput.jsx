

import React, { Component } from 'react'

export class WapInput extends Component {
    state = {
        input: {
            txt: ''
        }
    }

    handleChange = ({ target }) => {
        const field = target.name
    }

    render() {
        return (
            <div>

            </div>
        )
    }
}
